function soma(n1=0, n2=0){ //Pode ser passado parâmetros "default" simplesmente atribuindo dentro dos parenteses. Assim, caso um parametro não seja passado, ele pega o default e não quebra a função
    return n1+n2
}

console.log(soma(2,5))